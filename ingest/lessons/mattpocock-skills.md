# mattpocock-skills

The third leading pack (complementary; authoring convergence + the ephemeral-handoff positioning datapoint).

**Informational — no action now**
- **`handoff` is deliberately ephemeral — the sharpest confirmation yet that koan owns durable continuity, and the third leg of the leaders arc.** Pocock's `handoff` skill writes its doc to "the temporary directory of the user's OS - **not the current workspace**" — throwaway, not git-tracked, no budgets, no archive, no cold-resume ritual, no decision log. Where agent-skills *conceded* cross-session memory is unsolved (D-025) and superpowers shipped dated-but-unmanaged spec docs (D-026), Pocock puts continuity **out of scope on purpose**. agent-skills' comparison doc named exactly these three (itself, superpowers, Pocock) as the leading packs that haven't solved durable memory; koan has now ingested all three, and the strongest one routes handoffs to `/tmp`. Corollary for D-007/README: koan composes with all three (no continuity lifecycle to collide with) and the composition story is now three-for-three. · mattpocock-skills productivity/handoff/SKILL.md · _logged for D-007 and README positioning, no action_.
- **The "two loads" vocabulary for the invocation split — crisper words for a decision koan already makes with a gate.** `writing-great-skills`/`.agents/invocation.md` frame skill granularity as two costs: **context load** (a model-invoked `description` sits in the window every turn) vs **cognitive load** (a user-invoked skill costs the human's memory — cured by a *router* skill when they multiply). koan resolves its core-vs-lifecycle split via D-013's delta gate (context load, *measured* not argued) and has no user-invoked skills (no cognitive-load half), so the framing renames a decision koan already makes more rigorously. Note the design refinement over superpowers: Pocock's `ask-matt` router is **user-invoked** (zero context load), where superpowers' `using-superpowers` spends ~480 words every session — a sharper router koan still has no need for. · mattpocock-skills writing-great-skills/SKILL.md · _no change_.

**Observed-working (convergent validation — no change, logged as evidence)**
- **"Negation backfires — prompt the positive" — third independent arrival at form-follows-failure.** `writing-great-skills`' *Negation* failure mode ("steering by prohibition backfires: _don't think of an elephant_ names the elephant… prompt the **positive**; keep a prohibition only as a hard guardrail you can't phrase positively, and pair it with what to do instead") is the finding koan adopted the **same day** as "Form follows failure" from superpowers (D-026). superpowers *tested* it (prohibition arm produced MORE unwanted content than no-guidance control); Pocock states it as a named failure mode; koan carries it in `distill.md`. Third derivation, zero counter-evidence. · mattpocock-skills writing-great-skills/SKILL.md · _no change_.
- **The no-op test — koan's D-013 delta gate stated for prose, and koan is ahead.** `writing-great-skills`' pruning rule: "does it change behaviour versus the default?" applied sentence-by-sentence (a weak leading word like _be thorough_ is a no-op; delete the whole sentence, don't trim it). That is koan's D-013 "baseline already passes ⇒ cut" and Stage 0 budget gate — except koan has a *deterministic* gate + behavior probes where Pocock has a manual read. Same discipline, koan systematized it. · mattpocock-skills writing-great-skills/SKILL.md · _no change_.
- **ADR-sparingly = koan's "real choices only", another independent arrival.** `domain-modeling` offers an ADR only when all three hold: hard-to-reverse **and** surprising-without-context **and** the result of a real trade-off — else skip. That is koan's DECISIONS "keep it to real choices, not mechanical constraints" + koan-wrap step 3's decision test ("a line with a reason you chose it over something else is a decision"). koan's is more developed: permanent IDs, archive lifecycle, revisit-if, budgets, lint. And its `CONTEXT.md` "glossary and nothing else, devoid of implementation details" mirrors koan's one-fact-one-home routing. · mattpocock-skills domain-modeling/SKILL.md · _no change_.
- **Leading words — koan already embodies the technique; named validation, adopt-if-a-gap-appears.** `writing-great-skills`' central authoring lever: a **leading word** is "a compact concept already living in the model's pretraining… it accumulates a distributed definition and anchors a whole region of behaviour in the fewest tokens." That is precisely how koan's own authoring works — the zen craftsperson persona, "the four koans", the `koan:` shortcut comment each recruit a pretrained prior to bind behavior cheaply. Human triage 2026-07-11 chose **log-as-evidence over a `distill.md` note**: koan already uses the technique masterfully, so a note would be a no-op by Pocock's own test (words without behavior change). Revisit only if a future authoring problem traces to a *missing* leading word (a rule that needs many words to bind). · mattpocock-skills writing-great-skills/SKILL.md · _no change (already-embodied)_.

**Deferred / rejected (triage)**
- **`CONTEXT.md` ubiquitous-language glossary — rejected on the fit-test.** Pocock's README calls it "the single coolest technique in this repo": a domain glossary that decodes project jargon so agents "use 1 word where 20 will do." But its payoff is *within-session verbosity reduction* — useful in a project that never has a second session — which is exactly the canonical fit-test exclusion (`distill.md`). A shared-vocabulary doc is a general build-quality/prompting technique, not koan's build-less or lose-less axis, and adding a fourth doc type is the sprawl koan resists. (Marginal cold-resume benefit exists but is subsumed by koan routing stable facts to CLAUDE.md.) · mattpocock-skills grill-with-docs + domain-modeling · _rejected (fit-test)_.
- **The SDLC + productivity skills wholesale.** grill-me / grilling / grill-with-docs / to-spec / to-tickets / triage / wayfinder / implement / tdd / code-review / codebase-design / improve-codebase-architecture / diagnosing-bugs / prototype / research / teach — a lifecycle cookbook, the same fit-test verdict as agent-skills (D-025) and superpowers (D-026). Where they overlap koan, koan already carries the rule via ponytail: `tdd`'s red-green = "one runnable check on non-trivial logic"; `diagnosing-bugs`' reproduce-minimise-hypothesise = "root cause, not symptom"; `codebase-design`'s deep-modules = build-less. · mattpocock-skills skills/* · _rejected (fit-test)_.
- **`ask-matt` router.** A user-invoked router over the other user-invoked skills — koan has no router (six description-routed skills, AGENTS.md core), same rejection as superpowers' `using-superpowers`. Noted above as a sharper *design* (user-invoked ⇒ zero context load) but koan has nothing to route. · mattpocock-skills ask-matt/SKILL.md · _rejected_.

**Meta-finding:** ingesting the *third* SDLC cookbook — after superpowers (the origin of the authoring technique) and agent-skills — yielded almost entirely convergence + fit-test rejection and **no confident `src/` change**. That low marginal return is itself koan's fit-test working: the leading packs occupy the lifecycle/authoring axis koan deliberately doesn't, and koan's continuity + anti-bloat axis stays uncontested across all three.
