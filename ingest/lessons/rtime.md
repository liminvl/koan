# rtime

Field evidence — skills-free + cross-tool; the docs carried the discipline alone.

**Proposed (net-new — triage)**
- **Stale git-state claims in HANDOFF — lintable when the tree is clean.** rtime's 06-13 reorg session wrote "**UNCOMMITTED**, doc reorganization … staged in the working tree but NOT committed. Review and commit before further work" into HANDOFF, then committed that very text (`ee78a8f`) — the claim was false at birth and sat false for 14 days in the *cross-tool* channel until a session spent a correction pass (`1d33b47`, "doc reorg is committed, not pending"). Deterministic rule: HANDOFF matches `/\buncommitted\b|\bnot (yet )?committed\b/i` AND `git status --porcelain` is empty ⇒ stale claim. Validated: fires on rtime's stale window, quiet on all four field repos today. Pinned FP class (the D-014 discipline): wrap-time claims with a **dirty** tree are TRUE and common — vibe-shield wrapped "uncommitted D-041 work" with a dirty tree (`5d31c71`) and the next wrap cleaned it (`49a78f9`); the clean-tree condition is what separates them. The lint matters precisely for the no-wrap/cross-tool case where nothing else re-audits the claim. · rtime `ee78a8f`→`1d33b47` · _adopted 2026-07-04_ → lint.mjs check 11 (claim regex + clean `git status --porcelain` ⇒ warn; any dirt, tracked or untracked, means the claim may be true — skip).
- **Cross-repo decision citations are ambiguous — check 10 both false-positives and silently mis-resolves them.** rtime's `docs/PLATFORM.md` cites the *platform repo's* decision log (D-004 surface, D-006 DB model, D-011 deploy path). Run against rtime, check 10 flags D-011 as phantom (FP — it exists, in the other repo) and says nothing about D-004/D-006 because they *collide with rtime's own unrelated entries* — rtime's D-006 is the superseded Neon-pooling decision, so a reader chasing PLATFORM.md's "D-006" lands on the wrong (dead) rationale. No lint can catch the collision without a convention: qualify cross-repo citations (`platform:D-006`), teach check 10 to skip the qualified form, and extend its warning message to suggest it (message text = zero token cost). rtime's docs need the qualifying edit either way (same class as first-year's dangling-refs next-step). · rtime docs/PLATFORM.md + DEPLOY.md vs docs/DECISIONS.md · _adopted 2026-07-04_ → check 10 skips `\w:D-nnn` (lookbehind) and its warning suggests the qualified form; rtime edit is a (human) next-step.
- **The cross-tool channel warning belongs in the templates.** rtime ran a month across two tools on git+docs alone, and both rule files carry the load-bearing sentence — CLAUDE.md: "Claude Code's auto memory and session files are LOCAL … NOT visible to Windsurf. Do not rely on them for handoff"; `.windsurfrules`: "do not assume either tool can see the other's chat history or memory." koan's CLAUDE.md template says "cold start or tool switch" but never states WHY the docs are the channel — a session on a multi-tool project can still park handoff-relevant facts in tool-local memory. One template comment line, zero skill tokens. · rtime CLAUDE.md Notes + .windsurfrules HANDOFF-001 · _adopted 2026-07-04_ → CLAUDE.md template comment under Start here.
- **Session-start git grounding.** rtime's START protocol reads HANDOFF *then* runs `git log --oneline -15` + `git status` "to see the exact current state before doing anything" — which is how the 06-27 session caught the stale-pending claim. koan-readback reads only the docs; a readback against a wrong HANDOFF plays back the wrong state with confidence. Candidates: one cross-check line in koan-readback step 1 (taxed skill spend, D-019) or a template "Start here" line (free channel, rtime's own solution). · rtime CLAUDE.md Session protocol · _adopted 2026-07-04_ → koan-readback step 2 ("git wins — flag the mismatch"), the batch's one taxed spend.
- **Reverse-routing line in the DECISIONS template header.** rtime's DECISIONS header self-routes both directions: "Keep it to *real* choices — not mechanical constraints (those go in CLAUDE.md)." koan-wrap step 3 has this rule, but mid-session writes never see wrap (the vibe-shield point-of-temptation lesson, third file: templates got the Gotchas→DECISIONS route and the HANDOFF cross-route in that batch; the DECISIONS→constitution route is the one still missing). One template line, zero skill tokens. · rtime docs/DECISIONS.md header · _rejected 2026-07-04 (triage)_ → wrap step 3 already carries the rule; the DECISIONS template header stays minimal.

**Observed-working / observed-failing (validates existing rules — no change, logged as evidence)**
- **The discipline transfers through docs alone — no skills installed.** Two different agent harnesses (Claude Code + Windsurf Cascade) kept one HANDOFF/DECISIONS lifecycle coherent for a month via prose rules only: wraps happened ("docs: update HANDOFF" ×15+), decisions got the full template shape (Decision/Why/Alternatives/Status/Revisit-if), supersession worked (D-006→D-008, annotated in place). Field validation for the AGENTS.md-class target thesis (D-002/D-003): the rules, not the skill runtime, carry the discipline. · rtime git log + docs · _no change_.
- **Header rules alone don't prune — the "Earlier state (still accurate)" tail.** rtime's HANDOFF header says "overwrite stale lines, don't append forever," yet a 7k inventory tail demoted under an "_Earlier state (still accurate):_" divider on 2026-05-31 (`72cefbb`) survived TWO later doc-hygiene sessions and rotted: it still claims Vercel auto-deploy, `@prisma/adapter-neon`, and Neon Postgres — all falsified by D-008 (Coolify, adapter-pg). A self-asserted freshness label ("still accurate") is never re-audited; content below a divider stops being overwritten. This is the failure wrap's prune-first step exists to prevent — evidence the wrap ritual earns its seat over header self-description, and the demote-not-delete move is the HANDOFF-internal twin of the `<details>` rule koan already has. · rtime docs/HANDOFF.md `72cefbb`→HEAD · _no change_ (no-wrap deployments are the gap; see the check-11 lesson).
- **Under-budget is not healthy.** rtime's HANDOFF sits at 10.5k/12k — passing the budget check — but ~7k of it is the rotted tail. Bloat and staleness are the same lines; the budget gate alone can't see it. · rtime docs/HANDOFF.md · _no change_ (noted as a known limit of budget-only checks).
- **Lint checks 1–7 behave on a third field repo.** Run 2026-07-04 against rtime: 4 warnings — missing archive (true: never archived, log at 7.2k doesn't need it), no `## Objective` (true: predates the template), no `## Checks` (true: has How-to-run but no done-definition), and the check-10 D-011 FP above. Checks 8/9 correctly silent (no retired rituals — skills were never installed; no archive candidates — D-006 superseded only 7 days ago). · koan lint.mjs vs rtime · _no change_.
