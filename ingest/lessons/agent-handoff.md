# agent-handoff

First third-party lose-less kit (independent reinvention; convergence on koan's own axis + the sharpest hook evidence yet).

**Candidate (net-new — triage)**
- **Anti-noop continuation guard.** The one genuinely novel operational rule: its continuation prompt forces the agent not to silently stall on "continue" — *"Do not answer 'No response requested' and do not stop silently. First state where you believe the previous turn stopped and the next concrete action, then continue."* A real failure (agents given a bare "continue"/"resume" no-op instead of resuming), which koan's resume ritual (`koan-readback`) doesn't guard. But it's a **prompt band-aid for a harness quirk** (Codex's "No response requested"), not a continuity-doc discipline: it doesn't change whether the docs carry state (koan's lose-less axis) — it patches how one harness reacts to a one-word prompt. · agent-handoff references/templates.md AGENT_SESSION_PROMPTS · _rejected 2026-07-11 (human triage — fit-test: off-axis harness workaround; useful generally, not build-less/lose-less). Revisit only if a real cold-resume run is observed silently stalling in the field._

**Informational — no action now**
- **Third field-proof of the deferred hook plumbing — and the first to validate the advisory-only (trigger-not-write) design for D-024's PreCompact specifically.** `handoff-watch.mjs` ships project-local (`.claude/hooks/` + `.claude/settings.json`) covering exactly koan's two deferred wants — **PreCompact** ("remind to update handoff before compaction if state changed" = D-024) and **SessionStart** (inject recovery reading order = D-020) — plus a **Stop** closeout reminder (koan-wrap's trigger). Crucially its `hooks.md` states koan's own D-024 rationale verbatim as a design law: *"hooks must check and remind; they must not generate handoff content, because closeout state needs repository context and agent judgment"* — i.e. the hook **triggers** the checkpoint, the agent **writes** it (always exit 0, `continue: true`, never block, never write files). After agent-skills' `SessionStart` `hooks.json` (D-025) and superpowers' `startup|clear|compact` bootstrap (D-026), this is the third harness shipping the plumbing and the sharpest match to D-024 — it strengthens the D-020/D-024/D-025 **Revisit if** lines (a claude-plugin target could ship these Claude-only, and the advisory-not-writing shape is now field-validated) but changes no decision text today. · agent-handoff references/hooks.md + templates/handoff-watch.mjs · _logged for D-020/D-024/D-025 revisit lines_.
- **Bootstrap script + session-prompts + read-only-permission merge — a distribution shape for D-007.** `bootstrap_handoff.py --platform both --layout multi --session-prompts --gitignore` is a conservative idempotent installer (dry-run, marker-merge, never-overwrite) in the same slot as handoff-kit's D-011 bundle and agent-skills' `npx skills` — another concrete reference for what koan's deferred publish path could look like. · agent-handoff scripts/bootstrap_handoff.py · _logged for D-007, no action_.

**Observed-working (convergent validation — no change, logged as evidence)**
- **"State matters more than history" — snapshot-not-diary, independently re-derived on a third-party kit.** Its Design Principle 2 (*"`AGENT_HANDOFF.md` is not a chat log"*), its "letting the file grow indefinitely with stale/contradictory notes" problem list, and its repair pass that "compresses stale history" are koan's four koans ("snapshot, not diary; overwrite stale lines") and koan-wrap's prune-first step. Every prior arrival at this rule was koan's own kit or a field deployment; this is the first *independent third party*. · agent-handoff SKILL.md Design Principles + quality.md · _no change_.
- **Repository-facts-first / `UNKNOWN` for the unverifiable = evidence-based done + "Proof = X".** Design Principle 1 forbids inventing facts and writes `UNKNOWN: <how to resolve it>` for anything unverified — koan's evidence-based-done (handoff adoption) plus the first-year/vibe-shield "name the closing evidence" convention on Not-yet-verified, arrived at independently. · agent-handoff SKILL.md Design Principle 1 + quality.md · _no change_.
- **Record the checks that were NOT run, and why = koan's Not-yet-verified + the skipped-trail.** `validation.md` records `passed | failed | not run` and the quality checklist calls out "Forgetting to record tests that were not run" as a defect — koan's HANDOFF "Not yet verified (claims not yet proven + what would prove them)" and the build-less skipped-trail (`skipped: X, add when Y`), on the validation axis. · agent-handoff references/templates.md validation.md · _no change_.
- **Decisions need reason AND evidence.** `decisions.md`'s `| Decision | Reason | Evidence |` separates the why from the grounding file/test/user-request — koan's DECISIONS `**Why**` carries both in prose, and every koan `D-0xx` in fact names its evidence ("evidence in runs/…", "fires on first-year's D-005"). koan's prose entries are *richer* than a 4-column table (they also hold Alternatives-rejected + Revisit-if a table can't), so the column is convergent, not a gap. · agent-handoff references/templates.md decisions.md · _no change_.
- **Don't force-migrate an existing handoff — repair from repo facts = koan-init re-run-safety.** *"Do not overwrite an existing `AGENT_HANDOFF.md` with a template … existing state must be repaired by reading repository facts"* is koan-init step 1/1a (never overwrite, additively backfill missing sections) and "seed from reality." Its idempotent marker-block merge (`AGENT_HANDOFF_PROTOCOL:START/END`, replace-between-markers) reaches the same re-run safety koan-init gets by a *different* mechanism (additive-by-section-presence) — and echoes koan's own `koan:core` markers. Convergent; no gap. · agent-handoff SKILL.md Idempotency Rules · _no change_.

**Positioning (A/B — no change, logged)**
- **8 files + a mandatory recovery reading order vs koan's 2 budget-gated docs — koan is ahead on the decision-log lifecycle.** agent-handoff splits state across `snapshot/workspace/decisions/work-log/validation/backlog/risks/archive` and therefore *needs* a 9-step "Recovery Reading Order" (read index→snapshot→risks→backlog first, the rest only when the task needs them) to keep cold-resume cheap. koan folds the same content into HANDOFF (Objective/Next-steps/Not-yet-verified/In-progress) + DECISIONS and reads all of it because each doc is small and budget-bounded — so the reading-order protocol is a *cost the finer split creates*, not a feature. And agent-handoff's `decisions.md`/`archive.md` have **no permanent IDs, no budgets, no keep-if-cited archive rule, no revisit-if** — exactly the lifecycle koan's D-001/D-019 built and the vibe-shield field run proved. The independent reinventor converged on koan's *content* but not its *lifecycle*: validation that koan's "fewer homes + budgets + permanent IDs" is the more disciplined design. · agent-handoff SKILL.md Multi-Document Recovery Contract + templates.md · _no change_.

**Deferred / rejected (triage)**
- **Stable-reading / anti-blind-paging protocol.** Its generated rules cap `Read` ranges (<240 lines), treat `offset` as a line number, and force re-anchoring with `rg -n`/`wc -l`/`sed -n` after offset drift, empty output, or an API termination. Clever within-session read-tool hygiene — but useful in a project that never has a second session and never risks over-building: the canonical fit-test exclusion (a general tool-use cookbook, koan's neither axis). · agent-handoff SKILL.md Design Principle 6 + README_en.md · _rejected (fit-test)_.
- **The bootstrap machinery as skill content.** Covered above as a D-007 distribution datapoint; as *koan skill text* it's setup/installer plumbing, off both axes. · agent-handoff scripts/bootstrap_handoff.py · _rejected (fit-test)_.

**Meta-finding:** the first third-party ingest on koan's **own** axis (lose-less), and it re-derives koan's continuity discipline nearly point-for-point — snapshot-not-diary, repo-facts-first/`UNKNOWN`, record-what-you-skipped, decisions-with-evidence, don't-force-migrate, repair-the-rot. Where the lifecycle-pack ingests (agent-skills/superpowers/mattpocock) converged on koan's *authoring/eval* discipline and conceded continuity unsolved, this kit shows the continuity axis itself has a stable, discoverable shape — and an independent builder who found it stopped exactly where koan's decision-log lifecycle (permanent IDs, budgets, archive keep-if-cited, revisit-if) begins. Net: **no `src/` change** (human triage 2026-07-11 rejected the one live candidate on the fit-test) — the fifth consecutive third-party/foreign-axis ingest to yield convergence + positioning rather than skill text, which is the fit-test working as designed.
