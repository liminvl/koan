# 9arty

Field evidence — sibling-seeded; retired-ritual rot hid in the log scaffolding.

**Proposed (net-new — triage)**
- **Retired-ritual references rot in the DECISIONS/archive scaffolding, not just the constitution — check 8's file scope was too narrow.** 9arty hand-wrote its own inline START/WORK/END session protocol (no ritual names in CLAUDE.md) yet still carries `handoff-wrap` in two template-boilerplate spots: DECISIONS.md's archived-index note ("Empty until… see `handoff-wrap`") and the archive header comment ("appended here by `handoff-wrap`"). Check 8 scanned only `[constitution, HANDOFF]`, so it stayed silent — the first field repo to prove the rot survives even a clean constitution. The discriminator that keeps this dogfood-safe: a retired name in *log scaffolding* (intro, headers, the index note) is a dangling instruction; one inside a `### D-nnn` entry body is history — koan's own D-005/D-009 name `radar-*` in their bodies and must not fire. · 9arty docs/DECISIONS.md + DECISIONS-archive.md vs koan `c1b86ea` · _adopted 2026-07-04_ → lint.mjs check 8 also scans DECISIONS.md + archive with `### D-nnn` entry bodies stripped (fires 2× on 9arty, stays clean on koan/first-year/vibe-shield/rtime). Pinned FP class (D-014): a `radar-*`/`handoff-*` name inside a decision *body* is a historical record — stripped, must not warn. 9arty's docs need the rename either way — a (human) next-step, same class as the field-repo doc fixes already queued.
- **Canonical examples can live in a sibling repo — check 5 called them typos.** 9arty's "Canonical examples to copy from rtime" points at bare `rtime/src/…` paths; rtime is a sibling repo at a different absolute location, so an agent working in 9arty literally can't open them. Check 5 flagged all five as "does not exist" — technically true (they're unresolvable locally) but the message implied a typo, not a cross-repo pointer. Parallels rtime's check-10 cross-repo *citation* FP exactly. Deterministic discriminator: a missing path whose first segment isn't a local entry (`rtime/…` when there's no local `rtime/`) is a sibling-repo pointer, not a typo. · 9arty CLAUDE.md Canonical examples · _adopted 2026-07-04_ → check 5 softens the message for cross-repo-looking paths ("looks like a sibling-repo path; use an absolute path or copy the example in-repo") — message text only, zero token cost, still warns (the pointer really is broken for a local agent). 9arty's docs need absolute paths — a (human) next-step.

**Observed-working / observed-failing (validates existing rules — no change, logged as evidence)**
- **Cross-repo decisions solved cleanly by ID-range partition + a legend — an alternative to `repo:D-nnn`.** Where rtime's PLATFORM.md cited a *different* repo's log ambiguously (the check-10 lesson), 9arty carried the sibling's decisions **verbatim under their original IDs** (D-001/002/005/006/007) and partitioned all new IDs to D-1xx, with a one-line header legend: "`D-0xx` = carried from rtime (same reasoning applies). `D-1xx` = new to 9arty." No collision, no ambiguity, no lint FP (each carried ID is a real single full entry in 9arty's own log). Field evidence that the cross-repo-citation problem has a doc-convention answer as well as the `repo:D-nnn` qualifier; koan's dup-ID and citation checks handle carried-verbatim IDs correctly. · 9arty docs/DECISIONS.md header · _no change_.
- **`_Last drift-audit: never_` — fourth field repo, second literal "never".** Same dead line as rtime: no auditor installed, so the template's drift-audit line sat reading "never" from day one. Confirms the D-009/handoff split — the line only does work where the `drift-audit` agent exists (first-year); everywhere else it's inert. koan already dropped it from templates; logged as the fourth datapoint. · 9arty docs/HANDOFF.md · _no change_.
- **Never-run code paths hide bugs — validates the "one runnable check" rule from the infra side.** 9arty's provisioning commit (`25314d8`) fixed three bugs *in the same commit that first ran the code*: `prisma.config` never loaded `.env.local`, Neon's direct URL was under a different env name, and the seed used a bare `PrismaClient()` invalid under Prisma 7's adapter model. All three were latent because nothing had executed those paths until infra day. Field evidence for build-less's "one runnable check on non-trivial logic" — the smallest thing that *runs* the path catches what review can't. · 9arty docs/HANDOFF.md Changes + `25314d8` · _no change_.
