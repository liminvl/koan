# first-year

Field evidence — a real deployed project, not skill text.

**Proposed (net-new — triage)**
- **Migration story when the kit renames/retires rituals.** Retiring the global `handoff-*` skills for koan silently broke first-year: its CLAUDE.md still instructs every session to run `handoff-wrap` / `/handoff-readback` / `/handoff-lint` / `/handoff-init`, and its HANDOFF/DECISIONS headers defer to "see `handoff-wrap`" for prune/entry rules — all dangling as of 2026-07-04 (only the `drift-audit` agent survives). The discipline kept limping along only because the habits were baked into CLAUDE.md prose. · first-year CLAUDE.md vs koan `eb7fdea` · _adopted 2026-07-04_ → lint.mjs check 8 (retired-ritual references in constitution/HANDOFF; fires 5× on first-year, 1× on vibe-shield, clean on koan). The richer `push.mjs` rewrite-on-push stays deferred — detection covers the failure. _Second datapoint 2026-07-04 (vibe-shield): its CLAUDE.md "Start here" step 3 still instructs `handoff-wrap` — dangling the same way. 2/2 field deployments rotted identically; the lint check is proven-needed, not speculative._
- **Routing needs a fourth home when the project has its own contract docs.** first-year field-extended the routing table: *specs own the rules, DECISIONS owns the rationale, HANDOFF owns the open questions* — mid-build decisions write the rule into the spec AND log only the why in DECISIONS. koan's routing table (Practice 2 / koan-wrap) doesn't say where a rule goes when a `specs/` contract exists, so a spec-driven project must rediscover this. · first-year CLAUDE.md golden rule 2 · _adopted 2026-07-04_ → fourth row in the koan-wrap routing table (the batch's one skill-token spend, +108 chars; wrap 5975/6000).
- **Archive-candidate lint rule (sessions under-archive).** first-year's DECISIONS.md carries 10 active entries near its 25k budget; several are status `implemented` from weeks/many-commits ago, yet only 2 ever got archived — implemented decisions still *feel* load-bearing so sessions never move them. Deterministic and lintable: status `implemented` + older than N commits (or a date threshold) → flag as archive candidate. · first-year docs/DECISIONS.md · _adopted 2026-07-04_ → lint.mjs check 9: status implemented/superseded + status-date ≥14 days + uncited in DECISIONS/HANDOFF + no **Load-bearing** line (the deterministic proxies for wrap's keep-rules; fires on first-year's D-005, clean on koan/vibe-shield). _Counter-datapoint 2026-07-04 (vibe-shield): there, archiving DID happen — one catch-up sweep ("archive D-003/007/008/012/015–025 to keep DECISIONS.md lean") then increments at nearly every wrap, 31/43 archived, log held at ~24k. The difference is budget pressure: vibe-shield's wraps archived when crossing 25k ("back under 25k" in its wrap notes); first-year never hit the budget, so nothing forced it. Refines the rule: the lint matters for the UNDER-budget case, and vibe-shield's de-facto candidacy signal — `implemented` AND uncited by any active entry (its load-bearing entries name their citers and stayed) — is sharper than an age threshold._
- **"Proof = X" on every Not-yet-verified item.** first-year's Not-yet-verified entries name the evidence that would close them ("bundle-verified only… Proof = watch the web preview / a native dev build"), turning the section from a guilt list into a work queue — and it let a cold session instantly rank what to re-verify. · first-year docs/HANDOFF.md · _adopted 2026-07-04 (as already-existing)_ → the HANDOFF template comment already reads "claims not yet proven + what would prove them"; a wrap clause restating it was rejected as duplicate tokens. _Second datapoint 2026-07-04 (vibe-shield): every Not-yet-verified entry names its proof ("needs a live crawler hit / a forged FCrDNS test", "needs real keys + a browser") and the section doubles as Next-steps #2–3 — the work-queue effect, observed twice. Note the HANDOFF template comment already says "+ what would prove them"; the remaining change is the wrap guidance._
- **Owner-tag human-gated next steps.** first-year idled 12 days because Next-step #1 (the human fun-test) was human-gated but formatted identically to agent work; a cold session couldn't see "nothing here is mine until the human plays." A `(human)` tag on gated steps is one token per line. · first-year docs/HANDOFF.md Next steps · _adopted 2026-07-04_ → HANDOFF template Next-steps comment ("tag steps only the human can do with (human)") — zero skill tokens. The Stage 2 probe idea (does a resuming agent decline the human-gated step?) stays open.

**Observed-working (validates existing adoptions — no change, logged as evidence)**
- **Cold-read reconstruction actually works.** A fresh session reconstructed the entire project state (phase, gates, risks, next action) from CLAUDE.md + HANDOFF + DECISIONS without opening source files — the Stage 2 continuity claim, observed in the field. · first-year, 2026-07-04 session · _no change_ → cite as field evidence for Practice 2; first-year is a candidate real-world Stage 2 fixture.
- **Derived-summary lines that name their source of truth.** first-year's HANDOFF Objective marks itself "a derived summary that drift-audit measures against — if it disagrees with the vision spec, the spec wins." Kills the two-sources-of-truth failure at the line level. · first-year docs/HANDOFF.md Objective · _no change yet_ → already implicit in koan's snapshot rules; consider stealing the phrasing if the Objective template ever grows a spec pointer.
- **Partial supersession annotations on decisions.** D-007 is annotated "(d) is now subsumed by [[D-009]] … the rest of D-007's tuning stands" — surviving parts stay binding, dead part points at its successor, no archaeology needed. · first-year docs/DECISIONS.md D-007 · _no change_ → koan-wrap's archive step already allows this; logged so the pattern isn't lost.
