# iams

Field evidence — healthiest deployment yet; the build-less half, observed in the wild.

**Adopted (2026-07-05 — one free-channel fix, surfaced by dogfooding the catch)**
- **check 11 matched a *mention* of "uncommitted", not the *act* of claiming it.** Writing up the iams catch made koan's own HANDOFF quote "uncommitted" repeatedly (documenting the check, quoting iams's claim); on a clean tree check 11 then fired on those quoted mentions — the exact mention-vs-act failure D-014 names, now on the lose-less channel. Fix: strip backtick/double-quoted spans before the regex, so a plain-prose claim (`## Changes this session (uncommitted…)`) still fires but prose that only discusses the word doesn't. Verified: koan's HANDOFF now clean, iams's plain `(uncommitted)` claim still caught; no fixture harness exists, so koan's own HANDOFF (which discusses the check) is the dogfood pin. Free channel, zero skill tokens, no new decision (D-019 policy). · koan `src/lifecycle/lint.mjs` check 11, surfaced by iams · _adopted_.

**Proposed (net-new — triage)**
- **(otherwise none).** Beyond the check-11 FP fix above, iams surfaced no new gap: koan-lint is otherwise clean on it — no retired-ritual rot (born on `/koan-*`, never installed `handoff-*`), no phantom IDs, no dead canonical paths, check 9 correctly silent (its `implemented` entries are all <14 days old). _Considered and rejected: linting stale "not pushed / not merged" claims the way check 11 lints stale "uncommitted." iams's "no git remote is configured" made its claim TRUE, so no field failure was observed, and checking push-state needs a remote + fetch — real machinery for a speculative class. Rejected as YAGNI; logged (D-014 discipline) so it isn't re-proposed._ · iams docs/HANDOFF.md · _no adoption_.

**Observed-working / observed-failing (validates existing rules — no change, logged as evidence)**
- **check 11 fires live on a fifth field repo — the field proof it was waiting for.** iams's HANDOFF calls this session's work "(uncommitted)" in two spots while the tree is clean and that exact work is committed at `10b812c` ("Unify admin/public header and move Reports…"). koan-lint check 11 catches it (clean `git status --porcelain` + the `uncommitted` token). This is the live catch koan's own HANDOFF "Not yet verified" asked for — "check 11 … still unproven in the field. Proof = a later readback/template run catching one live." Bonus dogfood: check 11 *also* fires on koan's OWN HANDOFF this session (the 9arty sitting it still called "uncommitted" was committed at `56352c0`) — two independent true positives, zero false positives. · iams docs/HANDOFF.md vs `10b812c` · _no change_ → closes koan's check-11 field-proof item.
- **Verification surfaces bugs review can't — three more, on a second field repo.** Every real-data / in-browser verification pass in iams's log flushed a latent bug the tests had missed until the path actually ran: `Country::iso2()` called a nonexistent `getByAlpha3()` (silently swallowed → every flag failed to render; D-011), `AlertLog` had no `$table` override so Eloquent queried `alert_logs` vs the real `alert_log` (every read/write would have failed the moment the model was used; D-012), and `fputcsv()`'s PHP-8.4 `$escape` deprecation (D-013). Same class as 9arty's `25314d8` (three bugs fixed in the commit that first *ran* the code) — build-less's "one runnable check on non-trivial logic; the smallest thing that runs the path catches what review can't," now observed in two independent field repos. · iams DECISIONS D-011/D-012/D-013 · _no change_.
- **Build-less reasoning happens and gets logged in the wild — the half prior field ingests under-covered.** iams's alternatives-rejected read like koan's ladder run for real: plain CSV controllers over a `maatwebsite/excel` dependency nobody asked for (D-013, native-before-dependency); one `RestrictsToAdmin` trait over 9× per-model Policy boilerplate (D-010, reuse over duplication); lifecycle as accessors + query scopes, never stored columns (D-008, derived-not-stored); "generic report builder (YAGNI); charts up front" and "digest / Slack / SMS — YAGNI until the office asks" both declined (D-015/D-016). Stage 1's scope axis found arms converge on the minimal build and couldn't measure a loc delta (D-016/D-017); iams is the qualitative field counterpart — evidence the discipline is real *and* that a koan-disciplined log captures the choice-and-why where a benchmark can't. · iams DECISIONS · _no change_.
- **Revisit-in-place keeps a near-budget log lean.** iams annotates decisions with `**Revisited YYYY-MM-DD:**` blocks appended to the same entry (D-008 flag-column drop, D-010 RelationManager gate, D-015 reports relocation) rather than minting a new decision or superseding-and-archiving — the partial-supersession pattern first seen on first-year's D-007, now the dominant idiom in a second repo. Because the status-date refreshes on revisit, the archive-candidate age gate (check 9) correctly treats a revisited entry as still active — no false archive-candidate flag on a live, recently-touched decision. Field validation of the check-9 age gate from the opposite direction to first-year (which under-archived). · iams DECISIONS D-008/D-010/D-015 · _no change_.
