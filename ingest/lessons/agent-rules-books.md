# agent-rules-books

Rule-compression pipeline (the authoring/compression meta-axis; strongest convergence yet on koan's own core activity).

**Candidate (net-new — triage)**
- **"Compress to decision-equivalence, not sentence-equivalence."** `PROCESS.md`'s stated goal: the compressed tiers must "preserve the rules that materially change an AI agent's design, architecture, refactoring, review, risk decisions, and repeated local implementation choices under context pressure" — *decision*-equivalent to the full source, never *sentence*-equivalent. It is the single sharpest one-line statement of what koan's whole compression discipline aims at (core markers, budgets, snapshot-not-diary, choice+why-only), and it names the *target* where koan's `distill.md` bar names only the *test* (shorter/clearer/stickier/better) and `form-follows-failure` names the *how*. · agent-rules-books `_rule-workbench/PROCESS.md` Goal · _log-only 2026-07-11 (human triage — already embodied twice: D-013 + the fit-test gate it on the authoring side, and koan-wrap's "snapshot not diary / one fact one home / choice + why only" is decision-equivalence on the lose-less side; a `distill.md` note would be a no-op by koan's own no-op test, exactly the mattpocock "leading words" precedent. Revisit only if a future compression call traces to a missing name for the target.)_

**Informational — no action now**
- **Three-tier budget model (`full` reference / `mini` on-demand / `nano` always-on) — koan's `koan:core` markers ARE the nano tier, chosen by measurement not a fixed bucket.** Their release matrix compresses e.g. Clean Code 13851 B / 220 rules (full) → 3804 B / 29 (mini) → 1235 B / 14 (nano, ~11× smaller), and `USAGE.md` routes them exactly as koan routes its skills: "use the smallest mechanism that still changes the agent's decisions… prefer skills for book-specific guidance… on-demand over global loading… nano only when it must be permanently always-on." koan already has a two-tier split — the `koan:core` markers (always-on AGENTS.md = their nano) and the full Claude skill (their mini/full) — but decides membership by D-013's delta gate rather than by three fixed budget buckets. Adding a middle tier is scope creep koan resists; the datapoint is that an independent kit re-derived koan's exact "smallest effective always-on surface" delivery rule. · agent-rules-books README Release Matrix + docs/USAGE.md · _logged for D-002/D-013 positioning, no action_.
- **`CRITICISM.md` names koan's two reasons to exist as this kit's top-two valid criticisms.** A self-graded critique list (most-valid-first, each with a validity score AND an honest "% solved" + `won't fix` resolutions). #1 "no clear measurement of improvement" (9/10 valid, self-graded 2/10 solved) is koan's entire Stage 0/1/2 + `triggers.mjs` apparatus — the evals this kit concedes it lacks. #8 "the approach may cause overengineering" (8/10) is koan's build-less axis, named as the risk of a rules-dump kit; their mitigation ("smallest effective mechanism, task-scoped loading") is koan's fit-test arrived at from the delivery side. #4 "a short set of project-specific rules may work better than a large generic set" (9/10) and #6 "rules should come from real project failures" (8/10, self-graded 3/10) are koan's field-ingest model (D-019 lessons from deployed projects) vs this kit's book-derived model. The strongest single positioning artifact ingested: a rules kit independently identifying that its own approach risks over-building and lacks outcome evals — the two exact gaps koan is built to close. · agent-rules-books docs/CRITICISM.md · _logged for D-007/README positioning, no action_.

**Observed-working (convergent validation — no change, logged as evidence)**
- **The rule-classification taxonomy + "default requires evidence" = koan's D-013 delta gate, independently re-derived from the compression direction.** `PROCESS.md` classifies every source rule before cutting (`default`/`book-thesis`/`decision-changing`/`micro-decision`/`conflict-resolver`/`trigger`/`checklist-only`/`framing`) and its founding heuristic is koan's D-013 verbatim: "**Human-obvious is not the same as agent-default** … require evidence from evals, review findings, or known model mistakes when labeling a rule `default`; if evidence is missing, do not use the label," plus the **removal test** ("if deleting a rule is likely to bring back a known bad habit, keep it"). That is D-013's cut heuristic ("a rule whose gate baseline already passes isn't earning its tokens") stated from the keep side, with the same evidence bar. After ponytail, handoff-kit, agent-skills, superpowers, and skillgrade, this is a sixth independent arrival at the delta gate — and the first from a *compression-pipeline* rather than an eval. · agent-rules-books `_rule-workbench/PROCESS.md` Interpretation Heuristics · _no change_.
- **Traceability discipline = koan's LESSONS-rejected-with-reason + DECISIONS Alternatives-rejected, on the compression axis.** Every retained rule gets an `M*`/`N*` id referencing source sections; every omitted rule gets an explicit disposition — `covered by Mx` / `covered by Nx` / `intentionally lost` — and "do not describe an omission only as `secondary` or `lower-yield`; show where the operational effect survived or state it was intentionally lost." That is exactly koan's LESSONS.md convention (every rejected/deferred lesson logged with its reason "so it isn't re-proposed") and DECISIONS' Alternatives-rejected + Revisit-if, arrived at for a many-books pipeline. koan already carries the discipline in two homes; the convergence is the "account for what you dropped, don't just drop it" instinct. · agent-rules-books `_rule-workbench/*/traceability.md` + PROCESS.md Traceability Rules · _no change_.
- **"Process bug vs book-specific miss — fix the process first, then re-run" = koan's D-002 + D-019.** `PROCESS.md`: "if review of one book exposes a recurring compression mistake, improve `PROCESS.md` first instead of hand-tuning that one book," with an explicit `process bug` (reusable omission class → fix the method, re-run all affected books) vs `book-specific miss` (one source's own thesis → fix that book only) split. That is koan's generate-from-one-source (D-002, "drift becomes impossible") and free-channel policy (D-019, a recurring field lesson becomes a lint rule, not a per-instance hand-fix) — the same "fix the generator, not the instance" meta-discipline, from a compression pipeline. · agent-rules-books `_rule-workbench/PROCESS.md` Process vs Book Diagnosis · _no change_.
- **The validation experiment reports the null alongside the win = koan's built-to-disprove + honesty notes.** Their one experiment (vibe-coded-crap refactor): `mini` rules scored ~74/100 vs 46/100 for merely naming the book — concrete operational rules beat an abstract reference (koan's "rules stay blunt; the zen voice lives in the framing," D-010). Crucially they publish it as "an early qualitative signal, **not a benchmark**" AND report the measurement that *didn't* move — a Reek code-smell count of 1083 vs 1077, "the advantage did not show up clearly in raw smell counts." Reporting the flat metric next to the favorable one is D-006's built-to-disprove ("if the arms converge, the benchmark says so") and koan's honesty-notes discipline (mark it, keep it, say why) — the caveman `HONEST-NUMBERS` convergence, now on a rules kit. · agent-rules-books README "Does it work?" · _no change_.

**Deferred / rejected (triage)**
- **Section Coverage Review — walk every source section and assign a disposition, mandatory even when no text changes.** A completeness gate on compression (kept-in-mini / merged-into-M* / kept-in-nano-only / intentionally-lost, for every section of `full.md`). Sound for a pipeline that must *exhaustively* compress one source into tiers — but koan's ingests read for *technique*, not to compress a book into a shipped tier, so an exhaustive per-section disposition walk is process built for a purpose koan doesn't have. Fails the fit-test (serves neither build-less nor lose-less; it is compression-pipeline QA). · agent-rules-books `_rule-workbench/PROCESS.md` Section Coverage Review · _rejected (fit-test / off-purpose)_.
- **The book rule-sets themselves as koan content.** Clean Code / Refactoring / APoSD / Clean Architecture / DDD ×3 / DDIA / PoEAA etc. — code-craft and architecture rule packs, useful in a project that never has a second session: the canonical fit-test exclusion, same verdict as the SDLC packs (D-025/D-026). Where they touch koan's restraint axis, koan already carries the rule via ponytail: APoSD's "prove a helper removes complexity; reject shallow wrappers" and "add complexity only when evidence justifies it" = build-less + "the best code is the code never written"; Refactoring's "stop when the change is easy and further cleanup would be speculative" = scope-to-what-changed + the skipped-trail; Clean Code's "leave touched code cleaner within scope, don't silently broaden the task" = koan's when-touching-code scope discipline. The kit's center of gravity (build code *well*) is orthogonal to koan's (don't build code you don't need). · agent-rules-books `*/*.{md,mini.md,nano.md}` · _rejected (fit-test)_.

**Positioning (A/B — no change, logged)**
- **A build-*better* kit that concedes it risks build-*worse* — the cleanest illustration yet that koan's axis is restraint, not craft.** agent-rules-books maximizes *applied* design principles (deeper modules, richer models, more boundaries); koan minimizes *unnecessary* construction. They are complementary and can stack (load a `mini` book skill for a refactoring pass; koan governs scope + continuity), but the kit's own `CRITICISM.md` #8 shows the tension: a rules-dump of architecture books, mis-scoped onto a small feature, *produces* the over-building koan exists to catch — which is why their mitigation had to become koan's exact rule ("smallest mechanism that still changes decisions," task-scoped, not global). koan has no craft-rules axis to collide with and no book-derived content to duplicate; the composition story (koan composes, doesn't compete) now spans SDLC packs (×3), an output-voice kit, two continuity kits, an eval framework, and a code-craft rules kit. · agent-rules-books README + docs/USAGE.md + CRITICISM.md · _logged for D-007 and README positioning_.

**Meta-finding:** the first ingest of a **rule-compression pipeline** — the exact meta-activity koan performs on itself (distill a large source into a minimal, decision-equivalent, always-on surface). It re-derives koan's authoring discipline point-for-point from the compression direction: the D-013 delta gate ("human-obvious ≠ agent-default; `default` needs evidence"; sixth independent arrival), the fit-test/no-op cut (the removal test), traceability-of-omissions (koan's LESSONS + Alternatives-rejected), fix-the-process-not-the-instance (D-002/D-019), the smallest-effective-always-on-surface delivery rule (koan's `koan:core` = their nano), and built-to-disprove honesty (reporting the flat metric, the self-graded criticism list). The queue's "first build-less candidate" framing resolves to: it is a build-*better* kit whose own criticism doc concedes it risks over-building — so its sharpest contribution is confirming that koan's axis is *restraint*, distinct from *craft*. Net: **no `src/` change** (human triage 2026-07-11 kept the one live candidate log-only) — the sixth consecutive third-party ingest to yield convergence + positioning rather than skill text, the fit-test working as designed.
