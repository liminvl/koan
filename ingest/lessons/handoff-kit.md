# handoff-kit

Independent parallel build (same author, same problem, diverged).

- **handoff-kit** — sibling repo `handoff-kit` (CLAUDE.md, docs/{HANDOFF,DECISIONS,DECISIONS-archive}.md, skills/handoff-{init,lint,readback,wrap}, agents/drift-audit.md, templates/, package/). A from-scratch reimplementation of the exact same continuity discipline, built by the same author on native Claude Code primitives (skills + a subagent + `CLAUDE.md` `@import`) rather than koan's generate-to-many-targets model. Its own D-012 (2026-07-04) distilled the **same first-year field deployment** koan ingested the same day — a natural cross-check on D-019's channel-picking policy. Ingested 2026-07-04.

**Adopted (2026-07-04 triage)**
- **Human sign-off line in Checks.** handoff-kit's `## Checks` template names the validation zones no command can prove ("visual/UX, prod-data migration is safe, the copy reads right") as an explicit `Human sign-off:` line, deletable if everything's machine-checkable. koan's Checks section (`src/templates/CLAUDE.md`) only ever lists automated commands — "done" silently over-claims what's actually covered when a project has a real human-only zone. One template line, zero skill tokens. · handoff-kit templates/CLAUDE.md (D-010) · _adopted_ → `human sign-off:` line added to `src/templates/CLAUDE.md`'s Checks section.
- **Greenfield interview/intake mode.** `handoff-init` step 3a: when there's nothing to seed from (empty repo, unrecoverable intent), interview the human a few questions at a time (problem + done-condition, who it's for/not for, constraints, settled decisions+reasons) and play the summary back for correction before writing any file — the front-end mirror of `handoff-readback`. koan-init only has the "seed from reality" path; a truly greenfield `/koan-init` today has to guess or leave the Objective blank. · handoff-kit skills/handoff-init/SKILL.md step 3a · _adopted_ → `koan-init.skill.md` step 3a ("greenfield or unclear intent — interview, don't guess"), skipped whenever real seeding is possible.
- **The fit-test one-liner for scope-gating features.** handoff-kit's D-010: *"if a feature would still be useful in a project that never has a second session, it doesn't belong in [this kit]."* Evaluated five power-user techniques against it, adopted two, rejected three cleanly. koan's own scope discipline (D-001's "two failure modes" framing, `ingest/distill.md`'s bar) had no single quotable test this sharp for the NEXT feature request. · handoff-kit docs/DECISIONS.md D-010 · _adopted_ → `distill.md`'s bar gained "The fit-test" (koan-flavored: build-less or lose-less, not general usefulness).
- **Deterministic SessionStart staleness hook, as a second reminder layer.** handoff-kit D-008 runs TWO non-blocking layers: a model-driven "when to suggest a ritual" block (koan already has this, one line under Start here) PLUS a deterministic `SessionStart` hook that fires regardless of the model, only at session edges. koan relies on the model-driven layer alone — a session that skips the readback nudge gets no second check. · handoff-kit CLAUDE.md D-008 + templates · _deferred_ → real hook config is per-target plumbing (Cursor/Codex/Copilot don't share Claude's hook system); revisit once a hook-capable target is built (echoes the ponytail hook-injection lesson already deferred for the same reason). _2026-07-10:_ a `PreCompact` checkpoint hook (fire a HANDOFF checkpoint before Claude Code compacts context) joins this same deferral — same per-target plumbing, same revisit trigger; the model-driven layer for it shipped instead as one ritual-line clause in `src/templates/CLAUDE.md` ("about to `/compact` → checkpoint first"), per D-019's free-channel policy.

**Informational — no action now**
- **Portable distribution bundle + cross-platform installer (D-011).** A `build-package.ps1` assembles a versioned zip (docs + both template sets + skills/agent + a bash/PowerShell installer wiring into `claude | codex | gemini | copilot | generic` config dirs), kept separate from the machine-local one-direction sync. Directly informs koan's own deferred D-007 ("npm/marketplace publish deferred") — a concrete shape for what "publish" could look like when that's revisited, including keeping the local push/sync path separate from the sharable-artifact path. · handoff-kit docs/DECISIONS.md D-011 · _logged for D-007, no action_.

**Observed-working (convergent validation — no change, logged as evidence)**
- **Two independent kits, same author, same source, landed on identical lessons.** handoff-kit's D-012 and koan's D-019 both distilled first-year on 2026-07-04 and both adopted: (1) a lint check for constitution references to a ritual that isn't installed (koan check 8 / handoff-kit's "rituals-resolve" check), (2) an archive-candidate lint check for `implemented`/`superseded` entries sitting active past a threshold (koan check 9, 14 days + uncited; handoff-kit, 30 days flat — the two thresholds/conditions are a live A/B, not a conflict to resolve), (3) a fourth wrap-routing home for projects with spec/contract docs (koan's wrap routing table; handoff-kit's wrap routing table, same wording nearly verbatim). Neither session saw the other's output first. Strong cross-validation that these three are real lessons, not overfits to one ingest session's judgment. · handoff-kit D-012 vs koan D-019 · _no change_ (the 14-day-vs-30-day divergence is worth watching if either ever produces a false positive/negative the other doesn't).
- **"Name the closing evidence" on Not-yet-verified converges a third time.** Already adopted into koan from first-year (2026-07-04) and independently present in handoff-kit's own templates before D-012 even ran (its D-012 notes "already in the templates — no change"). Three sightings (first-year, vibe-shield, handoff-kit) of the same convention with zero counter-evidence. · handoff-kit docs/HANDOFF.md · _no change_.
