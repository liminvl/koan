# caveman

Output-voice compression (a different axis; sharp bidirectional fit-test reject + honesty-discipline convergence).

**Informational — no action now (D-007 / README positioning)**
- **First ingest on a different axis — output-token efficiency, not lifecycle.** The three third-party ingests before this (agent-skills, superpowers, mattpocock) were all SDLC/lifecycle cookbooks; caveman is orthogonal — it shrinks the agent's *output prose* (and, via `caveman-compress`, input tokens in memory files), touching neither build-less (code volume) nor lose-less (continuity). The composition story now spans two axes: caveman shrinks what the agent **says**, koan shrinks what it **builds** and owns **continuity**; they stack cleanly (you can run both). Corollary for D-007/README: the "koan composes, doesn't compete" positioning extends past the leaders to a token-efficiency kit. · caveman README + skills/caveman/SKILL.md · _logged for D-007 and README positioning, no action_.
- **`docs/HONEST-NUMBERS.md` is a model for a "when this kit isn't worth it" page — sharper than koan's current README carries.** caveman publishes exactly when its *own headline metric* goes net-negative: the skill costs ~1–1.5k input tokens/turn, so on terse workloads it loses; it cites the adversarial GitHub issues where users measured the loss (#145/#506/#550) and says "They were right"; it labels estimates `est.` vs measured; it reports the full range (22–87%), not just the 65% headline; "Wanting the rock to work does not make the rock work." koan already carries a version of this (D-023's frontier-only stance = "koan doesn't help you on haiku"), but caveman's crisp *turn-it-off rule of thumb* ("normal reply shorter than ~1.5–2k output tokens → caveman probably costs you money") is sharper than anything koan's README states. Candidate README sharpening when D-007 is revisited. · caveman docs/HONEST-NUMBERS.md + README "Honest number warning" · _logged for D-007, no action_.

**Observed-working (convergent validation — no change, logged as evidence)**
- **`HONEST-NUMBERS.md` = koan's "built to disprove" + honesty-notes discipline, arrived at from a token-efficiency kit.** Publishing your own metric's failure modes, citing adversarial evidence, and separating measured from estimated numbers is D-006's benchmark ethos ("if the arms converge, the benchmark says so") and koan's "honesty notes + supersession" (mark an inflated/contaminated run, keep it, say why) — independently derived in a completely different domain. "Wanting the rock to work does not make the rock work" is koan's built-to-disprove in caveman voice. Strongest cross-domain arrival of the honesty discipline so far (the SDLC ingests converged on the *delta gate*; caveman converges on *benchmark honesty*). · caveman docs/HONEST-NUMBERS.md · _no change_.
- **Auto-Clarity carve-outs = koan's "When NOT to be lazy" consolidated-guardrail block.** caveman drops compression for security warnings, irreversible-action confirmations, multi-step sequences where omitted conjunctions risk misread, and any case where compression itself creates ambiguity — a single when-NOT-to block with the safety exceptions named in one place, structurally identical to koan's "When NOT to be lazy" + "Never." Both encode "the discipline has hard exceptions; never let it override a safety guard." Convergent. · caveman skills/caveman/SKILL.md "Auto-Clarity" · _no change_.
- **The no-invented-abbreviations rule is measurement-driven — evidence over intuition, koan's benchmark instinct as a micro-rule.** caveman forbids `cfg`/`impl`/`req`/`res`/`fn` and causal arrows (→) with a *measured* justification: "tokenizer split them same as full word: zero token saved, reader still decode. Full word cheaper AND clearer." A rule that looks like it serves the kit's goal (terseness) but was killed because the measurement said it doesn't — exactly koan's tokens-up/outcome-flat cut heuristic (D-006/D-013) applied to a wording choice. Craft admiration; convergent. · caveman skills/caveman/SKILL.md Rules + Intensity · _no change_.
- **`caveman-compress`'s preserve-exactly / compress split mirrors one-fact-one-home + "aphorisms survive compression."** Its rules hold code, URLs, paths, commands, technical terms, dates, and version numbers **byte-exact** while compressing only prose filler and merging redundant bullets — the token-level twin of koan's routing (load-bearing facts stay verbatim) and the four koans' "aphorisms survive compression," with a hard carve-out for anything a reader must decode precisely. Convergent instinct, different layer. · caveman skills/caveman-compress/SKILL.md · _no change_.

**Deferred / rejected (triage)**
- **The core caveman voice rule (terse output) — rejected on the fit-test; resolves the queued "output/voice rule" note.** A rule styling the agent's chat voice is useful in a project that never has a second session and never risks over-building — the canonical fit-test exclusion. koan has no output-voice axis, and adding one is precisely the scope creep koan exists to catch. koan's terseness targets the **docs and code** (budgets, snapshot-not-diary, the four koans), never the agent's conversational register. The 2026-07-04 queue note ("caveman may inform an output/voice rule") resolves to **no**. · caveman skills/caveman/SKILL.md · _rejected (fit-test)_.
- **`caveman-compress` applied to koan's continuity docs — considered and rejected; the compression-vs-clarity tradeoff cuts *against* lose-less.** This is caveman's one turf-touching feature: it rewrites CLAUDE.md/HANDOFF-class memory files terse to buy input-token headroom. Tempting as a fourth budget-relief lever next to koan's prune-stale + archive. Rejected, and the reason is bidirectional: caveman's **own** Auto-Clarity rule says drop compression for "multi-step sequences where omitted conjunctions risk misread" — decision *rationale* and handoff *next-steps* are exactly that content. koan's existing budget levers remove content that **should** go (stale facts overwritten, implemented decisions archived); compressing *live* prose trades cold-resume clarity for tokens on the very content continuity depends on. koan's docs are also born terse, so there's little slack to compress. The one safe scope for `caveman-compress` is **verbose user-authored memory files** — a different population than koan's prune-based, born-terse continuity docs. koan does **not** adopt "compress your HANDOFF." Logged (D-014 discipline) so it isn't re-proposed. · caveman skills/caveman-compress/SKILL.md vs koan budgets + snapshot rules · _rejected (anti-axis)_.
- **`caveman-stats` / statusline / `caveman-commit` / `caveman-review` / `cavecrew-*`.** Output-formatting + cost-telemetry + terse-persona subagents — off both koan axes. `caveman-commit`'s "why over what, ≤50-char subject" overlaps koan's evidence-based-done/commit hygiene only incidentally and koan already carries the substance. · caveman commands/ + skills/caveman-stats · _rejected (fit-test)_.

**Meta-finding:** caveman is the first ingest on a **different axis** than koan (output-token efficiency / voice vs build-less / lose-less) and the first with a **bidirectional** fit-test reject — not only does its core fail koan's axis, its one turf-touching feature (`caveman-compress` on continuity docs) actively *risks* the lose-less goal it appears to serve. Yet a genuinely excellent kit on a foreign axis still yielded strong convergent validation of koan's *honesty* discipline (D-006) — the honesty ethos generalizes past koan's own two axes. Net: **no `src/` change**, and the queued output/voice note resolves to a clean no. The fit-test working exactly as designed, now demonstrated against a kit koan actively admires rather than one it overlaps.
