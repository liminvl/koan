# vibe-shield

Field evidence — longest deployment; the archive lifecycle ran for real.

**Proposed (net-new — triage)**
- **Decision IDs are minted only at log time — planning docs must not pre-assign them.** vibe-shield's `docs/PROPOSAL-opmc-adoptions.md` pre-allocated candidate IDs D-036…D-041; every one was taken by other decisions before the proposals were built, and its HANDOFF now carries a standing warning ("all stale — renumber from the next free ID"). A collision class the dup-ID lint can't see (the phantom IDs live in a satellite doc). · vibe-shield docs/HANDOFF.md + PROPOSAL doc · _adopted 2026-07-04_ → lint.mjs check 10: a satellite doc under docs/ citing a D-id that resolves to no entry/stub, or carrying a full `### D-nnn` heading, warns (fires on vibe-shield's HANDOFF "renumber from D-044" note). The wrap clause was rejected — lint catches the act at zero token cost. Known limit: a pre-assigned ID that later collides *resolves* and stops warning; the rule fires in the window before collision, which is when it prevents the class.
- **Routing rules at the point of temptation, not just in the wrap skill.** vibe-shield's CLAUDE.md gotchas header carries its own routing test — "If a line has a *reason you chose it over something else*, it's a decision — move it to DECISIONS.md instead" — and its HANDOFF header cross-routes ("Stable facts belong in CLAUDE.md; decision rationale in DECISIONS.md"). koan's routing table lives only in koan-wrap; the templates self-describe but don't cross-route, so mid-session writes (which never see wrap) get no routing nudge. · vibe-shield CLAUDE.md invariants header · _adopted 2026-07-04_ → the Gotchas template comment gained the reason-over-alternative test; the HANDOFF template header gained the cross-route line. ~3 template lines, zero skill tokens.

**Observed-working (validates existing adoptions — no change, logged as evidence)**
- **The archive lifecycle works end-to-end under budget pressure.** 31 of 43 decisions archived (catch-up sweep + per-wrap increments), full text preserved verbatim, stub index in parity, active log held at ~24k/25k across ~20 sessions — koan-wrap step 4's reactive "over or close ⇒ archive under" trigger, observed doing exactly its job. The residual gap is the under-budget case (see the first-year archive-candidate lesson). · vibe-shield git log + DECISIONS.md/-archive.md · _no change_.
- **"Keep if cited" is what a real archive converges to.** The entries that stayed active (D-004/D-009/D-013/D-011) are precisely the ones whose Load-bearing lines name later citers ("cited by D-019/D-020/D-022/D-024"); everything implemented-and-uncited got archived. Field validation of wrap's keep-if-cited rule, and the basis for the sharper lint candidacy signal above. · vibe-shield docs/DECISIONS.md · _no change_.
- **Revisit-if lines fire in the field.** D-041 opens with "Take the documented D-029 revisit"; D-042/D-043 landed from live event-log telemetry — the exact condition D-003/D-009 named ("telemetry shows false positives / defaults under-react"). The template element is honored end-to-end, weeks later, by sessions that didn't write the original entries. · vibe-shield docs/DECISIONS.md D-041/D-042/D-043 · _no change_.
- **The skipped-trail act arises unprompted in the field.** HANDOFF's Next steps carries "Parked (decided, not built): 'Flag for review' … most-requested competitor capability but cuts against the pure pre-charge posture; needs its own decision before building" — a scope line declining the most-tempting feature, left by a handoff-only project with no build-less skill loaded. Evidence the trail convention captures something models do when the doc structure invites it. · vibe-shield docs/HANDOFF.md Next steps · _no change_.
